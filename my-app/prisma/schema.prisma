generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model VideoEntity {
  id          Int    @id
  videoId     String?
  title       String?
  description String?
  image       String?
  views       String?
  likes       String?
  token       String?
}
